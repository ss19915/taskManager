import React from 'react';
import TaskCreator from './TaskCreator';

const TaskCreatorContext = () => (
    <TaskCreator
        saveTask={() => {}}
        redirectHome={() => {}}
    />
);

export default TaskCreatorContext;